// ABOUTME: Application entry point for ProTempo.
// ABOUTME: Registers the root component with Expo's runtime.

import { registerRootComponent } from 'expo'

import App from './App'

registerRootComponent(App)
